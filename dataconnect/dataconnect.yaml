specVersion: "v1beta"
serviceId: "social-plan"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "social-plan-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
